{{- if .Values.inlinespc.serviceAccount.create -}}
# Copyright Siemens 2023
apiVersion: v1
kind: ServiceAccount
metadata:
  name: inlinespc
  labels:
    {{- include "opcenter-core.labels" . | nindent 4 }}
  {{- with .Values.inlinespc.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
