{{- if .Values.securityServices.serviceAccount.create -}}
# Copyright Siemens 2022
apiVersion: v1
kind: ServiceAccount
metadata:
  name: security-services
  labels:
    {{- include "opcenter-core.labels" . | nindent 4 }}
  {{- with .Values.securityServices.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
